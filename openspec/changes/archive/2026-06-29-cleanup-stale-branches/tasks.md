# Tasks

- [x] リモートブランチの一覧確認
- [x] マージ済みリモートブランチの削除（`geekjapan/address-opsx-review` を削除、他は PR マージ時に自動削除済み）
- [x] ローカルブランチの一覧確認と不要ブランチの削除（ローカルは main のみ — 対応不要）
- [x] `git remote prune origin` で stale ref 整理（3 refs pruned）
